# HANDOFF（PC移行メモ）

## このアプリは何か

速報ハブ：電車遅延・渋滞情報・地震/災害・天気の4ジャンルをジャンル選択→路線/地域の絞り込み→結果表示、という流れで一元的に確認できるWebアプリ。すべて無料のデータソースのみで構成する方針。

## 実装済みの機能

- ホーム画面（ジャンル選択）→ `/[genre]` 画面（ドリルダウン選択→結果表示）の画面遷移一式
- **天気**: 気象庁 `bosai/forecast` JSONを使い実データで動作（登録不要）
- **地震・災害**: P2P地震情報APIを使い実データで動作（登録不要）。地方ブロックでの絞り込み
- **電車遅延**: UI・APIルートは実装済みだが、データはサンプル固定値（後述の理由で実データ未接続）
- **渋滞情報**: UI・APIルートは実装済みだが、データはサンプル固定値（後述の理由で機能保留）
- ジャンル横断の正規化データ形式・簡易インメモリキャッシュ・API Route経由でのフロント⇔外部API連携の骨組み

## 未完了のタスク・既知の問題

1. **電車遅延の実データ化が未完了**
   - ODPT(公共交通オープンデータセンター)の開発者登録が必要（無料だが承認まで最大2営業日、氏名・住所等の入力が必要）
   - 登録・承認後にアクセストークンを取得し、`src/lib/sources/train.ts` の TODO 部分を実装する必要あり
2. **渋滞情報のジャンルは方針保留中**
   - JARTICの交通量APIは登録不要ですぐ使えることを確認済みだが、(a)直轄国道のみで高速道路は対象外、(b)路線名がなく緯度経度と観測地点IDのみ、という制約があり、「路線名で選ぶ」UIと両立しない
   - 代替の無料手段は探索済みだが見つかっていない（詳細はREADME参照）。方針が決まるまでモック表示のまま
3. **README.mdに未コミットの修正が1件ある**
   - ODPT開発者サイトの正しいURL(`https://developer.odpt.org/`、誤って`developer-dc.odpt.org`と書いていた)への訂正と、JARTICが登録不要である旨の訂正。ワーキングツリーには反映済みだが、このHANDOFF作成時点ではまだコミットしていない（今回のコミット対象はHANDOFF.mdのみのため）
4. 自動テストは未整備（型チェック`tsc --noEmit`とESLintのみ実施済み）

## ローカルでの起動手順

```bash
npm install
npm run dev
```

`http://localhost:3000` を開く。Node.jsが必要（未インストールなら [nodejs.org](https://nodejs.org/) からLTS版を入れる）。

このリポジトリ単体では `.claude/launch.json` は含まれていない（開発時はワークスペース側の設定ファイルでポート3100を使っていたが、それはこのリポジトリの外にあるファイルなので新PCでは不要）。

## 技術構成・主要ファイル

- Next.js 16 (App Router) + React 19 + TypeScript、DBなし・ステートレス
- `src/lib/types.ts`: ジャンル横断の正規化データ形式（`NormalizedItem`）
- `src/lib/genres.ts`: ジャンルごとの選択肢・メタ情報（単一の設定源。UIとAPIの両方がここを参照）
- `src/lib/sources/{weather,quake,train,traffic}.ts`: ジャンルごとの外部APIアダプタ（train/trafficは現状モック）
- `src/lib/cache.ts`: 無料APIへの過剰アクセスを避けるための簡易インメモリキャッシュ（TTL付きMap）
- `src/app/api/[genre]/route.ts`: フロントから叩くAPI Route。ジャンルIDで各アダプタに振り分け
- `src/app/page.tsx`: ホーム画面（ジャンル選択）
- `src/app/[genre]/page.tsx` + `src/components/GenreView.tsx`: ドリルダウン画面（サーバー側でジャンル設定を解決し、クライアント側で選択・fetch・表示を担当）
- `src/components/{GenreCard,ResultList}.tsx`: 共通UIパーツ

## 新PCで別途用意・設定が必要なもの

- Node.js（LTS）
- Git、および GitHub への push 用認証（GitHub CLIでの認証を推奨。パスワードを直接使う運用は避ける）
- （電車遅延を実データ化する場合）ODPT開発者登録・アクセストークン。取得後にコードへ組み込む際は環境変数化する想定（変数名は未確定・現時点でコードは環境変数を参照していない）
- 現時点でこのリポジトリは外部秘密情報（APIキー等）を一切必要としない。`.env*` は `.gitignore` 済みなので、今後追加する場合はそちらに置く

## 開発を続ける際の注意点

- **ジャンルの選択肢や表示ラベルは `src/lib/genres.ts` が唯一の情報源**。路線・地域を増やす/直す場合はここを編集すれば画面・API双方に反映される
- train.ts / traffic.ts に手を入れる際は、`GenreResult` の `isMock` フラグと `ResultList` のモック表示バナーの整合を崩さないこと
- 渋滞情報の仕様（高速道路 or 国道か、路線名 or 地域か）を変える場合は、まずジャンルとしての方向性をユーザーと合意してから着手する（過去に何度か前提が覆っているジャンルのため）
- このプロジェクトが動いている開発環境はディスクI/Oが全体的に遅い傾向があるため、`npm install` やビルド系コマンドは体感より時間がかかることがある
- 外部APIはいずれも無料・非公式または軽量な利用規約に基づくものが多く、仕様変更で突然壊れる可能性がある前提でコードを書くこと（例外処理・空データ時の表示は既に考慮済み）
