# HANDOFF（PC移行メモ）

## このアプリは何か

速報ハブ：電車遅延・渋滞情報・地震・天気の4ジャンルをジャンル選択→路線/地域の絞り込み→結果表示、という流れで一元的に確認できるWebアプリ。すべて無料のデータソースのみで構成する方針。

## 実装済みの機能

- ホーム画面（ジャンル選択）→ `/[genre]` 画面（ドリルダウン選択→結果表示）の画面遷移一式
- **天気**: お気に入り未設定なら神奈川県を表示。複数地点の表示、気象庁の予報区（東部・西部など。気象庁の予報がこの単位でしか出ないため）と市区町村（Open-Meteoのモデル予測。区域一覧は気象庁area.json、座標は国土地理院の住所検索）を選べる。気象庁 `bosai/forecast` JSON（天気・気温・降水確率）とOpen-Meteo（降水量の予測。気象庁の予報にはないため）で、3日分のアイコン・気温と、降水確率・降水量を6時間ごとの表で表示（バーは「量」に見えて分かりにくかったため廃止。注意書きも不要とのことで出していない）。降水量は時間ごとの予測を6時間ずつ合計（登録不要）。`WeatherCard.tsx`
- **地震**: P2P地震情報APIの地震情報（コード551のみ。津波予報・緊急地震速報は未取得なので「地震・災害」から改名）を実データで表示（登録不要）。地方ブロックでの絞り込み。震源が題名、震度がバッジ
- **お気に入り設定** (`/favorites`): 天気の地点（複数）、電車の初期エリアとお気に入り路線（電車ページ上部に平常運転を含む現在の状態を表示）、地震・渋滞の初期選択。保存先はブラウザのlocalStorageのみ（DBなし。他の端末とは共有されない）。`src/lib/favorites.ts`
- **更新ボタン**: 全ページにあり。自動更新はなく、サーバー側キャッシュ（地震30秒・天気/電車60秒・市区町村の天気30分）があるので直後は同じ内容が返ることがある
- **電車遅延**: 路線を選ぶのではなく、ODPTの運行情報から「いま実際に遅れ・運休が出ている路線」を全事業者分まとめて重大度順に表示（「お知らせ」・事前の注意喚起・工事や明日以降の運休予定は除外。判定は `train.ts` の `assessTrouble`。文面ベースなので、想定外の言い回しで取りこぼす/拾いすぎる可能性がある）（エリアと事業者での絞り込み・更新ボタンあり。初期表示は首都圏。「首都圏」は一般的な狭義の1都3県(東京・神奈川・埼玉・千葉)で、法律上の1都7県ではない。区分は `train.ts` の `JR_EAST_METRO_LINES` 等と東武の `TOBU_OUTSIDE_METRO_LINES`、詳細はREADME）。並び順は首都圏の中心に近い路線ほど上（`rankOf` と `JR_EAST_CORE_LINES` / `CORE_LINES_BY_OPERATOR`。段階の分け方は私が決めたものなので、違和感があれば表を直す）。状態はバッジ+凡例で表し、色だけに頼らない。対象はJR東日本・東京メトロ・都営・東急・京王・西武・東武。小田急・京成はODPTに運行情報がなく対象外（詳細はREADME）
- **渋滞情報**: UI・APIルートは実装済みだが、データはサンプル固定値（後述の理由で機能保留）
- ジャンル横断の正規化データ形式・簡易インメモリキャッシュ・API Route経由でのフロント⇔外部API連携の骨組み

## 未完了のタスク・既知の問題

1. **電車遅延の注意点**
   - 東京メトロの実データ取得には `.env.local` の `ODPT_CONSUMER_KEY` が必要。未設定なら東京メトロは表示されず、画面に注意書きが出る
   - JR東日本・東急・京王・西武・東武は「公共交通オープンデータチャレンジ2026」限定データ（別ホスト api-challenge.odpt.org・別トークン `ODPT_CHALLENGE_KEY`）。実データ取得を確認済み（新PCへ移すときは `.env.local` を作り直してトークンを再設定する）。未設定の間は表示されない。西武は事業者全体の1件のみ返す
   - 小田急・京成はODPTに運行情報の提供なし（実データ化する別ソースは未調査。rti-gikenの遅延JSON APIは2022年に終了済みで使えない）
   - チャレンジ限定データは「チャレンジ終了まで」（〜2027-03-12）の提供で、限定ライセンスにアプリ公開義務等がある。エントリー時に原文確認が必要
   - JR東日本は路線マスタ(`odpt:Railway`)が取れないため、路線名を `train.ts` の `JR_EAST_NAMES` の対応表で補っている。新しい路線IDが増えた場合は対応表に追加する（なければ文面の先頭から路線名を推定する）。エリア区分も同様で、`JR_EAST_METRO_LINES` / `JR_EAST_KITAKANTO_KOSHINETSU_LINES` / `JR_EAST_OTHER_LINES` にない路線は東北扱いになる
2. **渋滞情報のジャンルは方針保留中**
   - JARTICの交通量APIは登録不要ですぐ使えることを確認済みだが、(a)直轄国道のみで高速道路は対象外、(b)路線名がなく緯度経度と観測地点IDのみ、という制約があり、「路線名で選ぶ」UIと両立しない
   - 代替の無料手段は探索済みだが見つかっていない（詳細はREADME参照）。方針が決まるまでモック表示のまま
3. 自動テストは未整備（型チェック`tsc --noEmit`とESLintのみ実施済み）

## ローカルでの起動手順

```bash
npm install
npm run dev
```

`http://localhost:3000` を開く。Node.jsが必要（未インストールなら [nodejs.org](https://nodejs.org/) からLTS版を入れる）。

このリポジトリ単体では `.claude/launch.json` は含まれていない（開発時はワークスペース側の設定ファイルでポート3100を使っていたが、それはこのリポジトリの外にあるファイルなので新PCでは不要）。

## 技術構成・主要ファイル

- Next.js 16 (App Router) + React 19 + TypeScript、DBなし・ステートレス
- `src/lib/types.ts`: ジャンル横断の正規化データ形式（`NormalizedItem`）
- `src/lib/genres.ts`: ジャンルごとの選択肢・メタ情報（単一の設定源。UIとAPIの両方がここを参照）
- `src/lib/sources/{weather,quake,train,traffic}.ts`: ジャンルごとの外部APIアダプタ（train/trafficは現状モック）
- `src/lib/cache.ts`: 無料APIへの過剰アクセスを避けるための簡易インメモリキャッシュ（TTL付きMap）
- `src/app/api/[genre]/route.ts`: フロントから叩くAPI Route。ジャンルIDで各アダプタに振り分け
- `src/app/page.tsx`: ホーム画面（ジャンル選択）
- `src/app/[genre]/page.tsx` + `src/components/GenreView.tsx`（天気は `WeatherView.tsx`）: ドリルダウン画面（サーバー側でジャンル設定を解決し、クライアント側で選択・fetch・表示を担当）
- `src/app/favorites/page.tsx` + `src/components/FavoritesView.tsx` + `src/lib/favorites.ts`: お気に入り設定（localStorage）。`useSyncExternalStore` で読み、初回描画中は `ready=false` にして無駄な取得を避けている
- `src/lib/sources/areas.ts` + `src/app/api/places/[area]/route.ts`: 市区町村の一覧と座標
- `src/components/ui.tsx` + `ui.module.css` + `src/app/globals.css`: 全ページ共通のUI部品（ページ枠・ボタン・チップ・カード・バッジ・凡例）と配色の変数。新しいページ・部品を足すときはここを使い、ページごとに別のスタイルを作らない
- `src/components/{GenreCard,ResultList}.tsx`: 共通UIパーツ

## 新PCで別途用意・設定が必要なもの

- Node.js（LTS）
- Git、および GitHub への push 用認証（GitHub CLIでの認証を推奨。パスワードを直接使う運用は避ける）
- ODPTアクセストークン（東京メトロの運行情報に必要）。プロジェクト直下の `.env.local` に `ODPT_CONSUMER_KEY=<トークン>` として置く。`.env*` は `.gitignore` 済みでコミットされない。新PCでは再作成が必要
- チャレンジ限定データ用の別トークン `ODPT_CHALLENGE_KEY`（JR東日本・東急・京王・西武・東武用）も同じ `.env.local` に置く
- ODPTトークン以外に外部秘密情報は不要。天気・地震は登録不要

## 開発を続ける際の注意点

- **ジャンルの選択肢や表示ラベルは `src/lib/genres.ts` が唯一の情報源**。路線・地域を増やす/直す場合はここを編集すれば画面・API双方に反映される
- train.ts / traffic.ts に手を入れる際は、`GenreResult` の `isMock` フラグと `ResultList` のモック表示バナーの整合を崩さないこと
- 渋滞情報の仕様（高速道路 or 国道か、路線名 or 地域か）を変える場合は、まずジャンルとしての方向性をユーザーと合意してから着手する（過去に何度か前提が覆っているジャンルのため）
- このプロジェクトが動いている開発環境はディスクI/Oが全体的に遅い傾向があるため、`npm install` やビルド系コマンドは体感より時間がかかることがある
- 外部APIはいずれも無料・非公式または軽量な利用規約に基づくものが多く、仕様変更で突然壊れる可能性がある前提でコードを書くこと（例外処理・空データ時の表示は既に考慮済み）
